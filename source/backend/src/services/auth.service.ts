export async function register(validatedData: unknown) {
  // Implement registration service logic here.
  // Example: persist validated data and return the created user.
  return validatedData
}
